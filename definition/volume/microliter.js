'use strict'

// var volume = require('./cubic-meter')

// volume.set('microliter', 'cubic-meter', 1 / 1000000000)

// module.exports = volume

module.exports = ['microliter', 'cubic-meter', 1 / 1000000000]
