'use strict'

// var volume = require('./cubic-meter')

// volume.set('cubic-centimeter', 'cubic-meter', 1 / 1000000)

// module.exports = volume

module.exports = ['cubic-centimeter', 'cubic-meter', 1 / 1000000]
