'use strict'

// var volume = require('./cubic-meter')

// volume.set('dry-barrel', 'cubic-meter', 1 / 8.6484898)

// module.exports = volume

module.exports = ['dry-barrel', 'cubic-meter', 1 / 8.6484898]
