'use strict'

// var volume = require('./cubic-meter')

// volume.set('cubic-decimeter', 'cubic-meter', 1 / 1000)

// module.exports = volume

module.exports = ['cubic-decimeter', 'cubic-meter', 1 / 1000]
